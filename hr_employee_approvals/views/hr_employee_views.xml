<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_employee_form_inherit_approvals" model="ir.ui.view">
        <field name="name">hr.employee.form.inherit.approvals</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <field name="approval_state" invisible="1"/>
                <field name="user_can_approve" invisible="1"/>
                
                <button name="action_submit_approval" string="Submit for Approval" 
                        type="object" class="oe_highlight"
                        invisible="approval_state != 'draft'"/>
                        
                <button name="action_approve_step" string="Approve" 
                        type="object" class="oe_highlight"
                        invisible="not user_can_approve"/>
                        
                <button name="action_reject_step" string="Reject" 
                        type="object" class="btn-secondary"
                        invisible="not user_can_approve"/>
            </xpath>
            
            <xpath expr="//page[@name='hr_settings']" position="after">
                <page string="Approvals" name="approvals">
                    <field name="approver_ids">
                        <list editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="user_id"/>
                            <field name="status" decoration-info="status == 'new'"
                                   decoration-warning="status == 'pending'"
                                   decoration-success="status == 'approved'"
                                   decoration-danger="status == 'cancel'"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
